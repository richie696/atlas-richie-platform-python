# Component contract template

Every new component must specify: public API and compatibility rules;
configuration inputs and defaults; error categories; behavioral semantics;
provider/adapter boundaries; observability data and redaction; and conformance
tests with their verification status.  Explain what a consumer can do, rather than
how internal objects are initialized.
